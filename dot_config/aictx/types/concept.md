## This chat is about a concept (`type:concept`)

Bias the notes you keep toward judgement, not mechanics:

- What it is — one tight definition.
- When to use it — the situations where it's the right call.
- When NOT to use it — failure modes and cheaper alternatives.
- Trade-offs against the obvious alternatives.
- A concrete example that made it click.
