## This chat is incubating (`stage:incubating`)

This is an idea space that will move to its own repo once it becomes real.
While it's still ideas, keep capturing here as normal. But watch for the
handoff trigger: **when implementation actually starts**, the new repo becomes
the home. At that point —

- Write the carry-over decisions and rationale into a clean summary file.
- Set `successor:` on `.aictx` line 1 to the new repo path/URL.
- Add the `archived` tag and stop adding new material here.

Don't fork work across both places; once handed off, this chat is a pointer.
