## This chat is about a tool (`type:tool`)

Bias the notes you keep toward operating the tool:

- Installation — where it lives, how it's invoked.
- Configuration — the exact settings that matter and what each does.
- Working examples — copy-paste commands that actually ran here.
- Gotchas — error messages seen and the fix for each.
- Version assumptions, where behaviour is version-dependent.
